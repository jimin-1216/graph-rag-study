# 260424 Results

## 시작 상태

- 어제까지 alias 해소와 추가 법령 수집, Neo4j 반영은 완료됐다.
- 현재 병목은 `검색 단계에서 질문 의도에 맞는 조문을 더 정확히 고르는 것`이다.
- 오늘 기준 질의는 `보고책임자의 역할은 무엇인가`로 고정한다.

## 오늘 진행한 것

- `260424` 작업 폴더 생성
- 오늘 계획 문서 작성
- 질문 의도 분류 초안 작성 착수
- `주체 일치 가중치 + 의도 충돌 감점 + 주체 불일치 감점` 규칙 문서화
  - `study/260424/ranking_rules_v1.md`
- `주체 일치`를 형태소 분석 없이 어떻게 점수화하는지 문서화
  - `study/260424/subject_matching_notes.md`
- 주체 정규화 레이어 추가
  - `보고책임자 / 보고 책임자 / 보고의 책임자`를 canonical subject로 묶도록 반영
- `run_graph_retrieval_demo.py`를 feature/weight 기반 랭킹 구조로 개편
  - 의도 분류: `role / deadline / procedure`
  - feature 추출: 제목 일치, 본문 초반 일치, 본문 반복 등장, 열거 패턴, 기한 표현, 부정 제목, 다른 주체 감점
- 대표 질의 3개로 재실행
  - 출력: `workspace/queries/graph_retrieval_eval_2026-04-24.md`
- `보고책임자의 역할은 무엇인가` 질의에서 `제7조 (보고책임자)`가 최상위로 올라오는 것 확인
- `은행장`, `이사회`, `감사` 같은 다른 주체 역할 조문은 감점으로 뒤로 보내는 1차 규칙 확인
- 주체 정규화 스니펫으로 `보고책임자 / 보고 책임자 / 보고의 책임자`가 같은 canonical subject로 묶이는 것 확인
- `procedure` 질의 보정 착수
  - `보고내용`, `보고대상`, `보존대상` 감점 강화
  - `절차`, `방법`, `제도운용`, `보고시기 및 방법` 가산 강화
  - `이사회`, `은행장`, `감사` 같은 다른 주체 조문 감점 추가
- `run_graph_retrieval_demo.py`에 feature/weight 기반 랭킹 반영
  - `role`, `deadline`, `procedure` 의도 분기
  - 주체 일치: 제목 / 본문 초반 / 본문 전체 분리
  - 감점: 보존/시행일/보고내용 등 의도 충돌 제목
  - 감점: `이사회`, `은행장`, `감사` 등 다른 주체 조문
- 리포트 재생성
  - `workspace/queries/graph_retrieval_eval_2026-04-24.md`

## 중간 결과

- `의심거래 보고 절차는 어떻게 되나`: issue `0`
- `고액 현금거래 보고 기한은?`: issue `0`
- `보고책임자의 역할은 무엇인가`: issue `0`

특히 `보고책임자의 역할은 무엇인가` 질의에서

- `제7조 (보고책임자)`가 최상위로 올라옴
- `제5조 (은행장)`은 `wrong_subject_title`, `wrong_subject_lead` 감점으로 뒤로 밀림
- `보고 책임자`, `보고의 책임자` 같은 표현 변형도 같은 주체로 처리할 준비를 마침

즉 주체가 틀린 역할 조문을 뒤로 보내는 1차 규칙은 효과가 있었다.

## 현재 판단

- 이제 문제는 alias보다 retriever 정밀화다.
- 특히 `역할/책임` 질의는 제목, 표제어, 주체, 서술 패턴을 함께 봐야 한다.
- 현재 1차 랭킹은 작동하지만, 아직 `절차` 질의에서 `보고내용`과 `절차/방법`을 완전히 구분하지는 못한다.
- 위 보정 규칙은 코드에 넣었지만, Neo4j가 꺼져 있어 재실행 검증은 아직 못 했다.

## 오늘 마무리 상태

- 랭킹 규칙 1차 구현 완료
- 주체 정규화 레이어 반영 완료
- 문서화 완료
- Neo4j가 중간에 내려가서 마지막 변형 질의 전체 재실행은 못 했음

## 집에 가서 이어서 할 일

1. Neo4j 켜고 변형 질의 재실행
- `보고책임자의 역할은 무엇인가`
- `보고 책임자의 역할은 무엇인가`
- `보고의 책임자는 무엇인가`

2. `procedure` 질의 보정
- `보고내용` 감점
- `절차/방법/제도운용` 가산 강화

3. 결과 리포트 다시 저장
- `workspace/queries/graph_retrieval_eval_2026-04-24.md`

4. 오늘 변경분 커밋 여부 정리

## 다음 기록 예정

- 기준 질의 실패 원인 정리
- 의도별 후보 생성 신호 정리
- 랭킹 규칙 반영 결과
