# 작업일지 - 2026-05-08

## 작업 내역

### [graph-rag-study] 내규 파서 품질 개선 (자금세탁방지업무 취급규정)

- `normalize_internal_rules.py` 파서 버그 5건 수정
  - `ARTICLE_RE` 정규식 수정: `제N조의M` 패턴 매칭 (`제6조의2`, `제26조의2`, `제28조의2`)
  - `STRUCTURE_RE`에 `###` Markdown heading 접두사 허용
  - `DELETED_ARTICLE_RE` 신규 추가: `제N조 삭제<날짜>` 형태 포착
  - `in_supplementary` 플래그 도입: 부칙 내 조문이 본문 article로 혼입되는 문제 차단
  - `strip_revision_tags()` 함수 추가: heading 제목의 `<개정>/<신설>` 태그 제거

### 재파싱 결과 (aml_policy)

- 조문: 28 → 32개 (+6 신규 포착, -2 오류 제거)
  - 신규: 제6조의2(준법감시인), 제26조의2(모니터링 체계 등), 제28조의2(보안관리), 제9·10·11조(삭제)
  - 제거: 부칙 내 제1조(시행일), 제2조(특례) — 본문에서 분리
- relation: 197 → 201개
- heading 태그 잔류: 4건 → 0건
- 부칙 혼입: 2건 → 0건

### 미커밋 작업분 정리 (4/23~24)

- 4/23 alias 해소 파이프라인 + 추가 법령 수집 작업분 커밋
- 4/24 retrieval 랭킹 규칙 작업분 커밋

### 산출물

- `study/260508/parsing_improvement_report.md` — before/after 비교 리포트 + 전체 조문 구조도
- `data/processed/internal_rules/internal_units.json` — 재파싱된 unit 데이터
- `data/processed/internal_rules/internal_relations.json` — 재파싱된 relation 데이터

## 이슈

- PDF 원본 줄바꿈 깨짐 (`사 전검토`, `법 률`) — opendataloader 추출 단계 문제, clean_text로 공백 정규화는 되지만 단어 내 공백 잔존
- `가/나/다` 목 단위 일부 미분리 케이스 존재
- 부칙 내부 구조화(조문 분리) 미실시 — 현재 텍스트 덩어리로 저장

## 다음 할 일

- Neo4j 재적재 후 기존 대표 질의 3개 regression 체크
- 취급지침(aml_guideline)에도 동일 파서 수정사항 적용 검증
- unresolved alias 36건 중 핵심 재정리
